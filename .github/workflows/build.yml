name: Build Android AppBundle & iOS IPA

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
      # 1. Effettua il checkout del repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Installa l'ultima versione stabile di Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 'stable'

      # 3. Disabilita le analitiche di Flutter (opzionale)
      - name: Disable Flutter Analytics
        run: flutter config --no-analytics

      # 4. Installa le dipendenze di Flutter
      - name: Install dependencies
        run: flutter pub get

      # 5. Build Android App Bundle (file .aab)
      - name: Build Android App Bundle
        run: flutter build appbundle --release

      # 6. Carica l'App Bundle (.aab) come artifact
      - name: Upload Android AAB
        uses: actions/upload-artifact@v3
        with:
          name: myapp-aab
          path: build/app/outputs/bundle/release/*.aab

      # 7. Build iOS IPA senza code signing
      - name: Build iOS IPA
        run: flutter build ipa --release --no-codesign

      # 8. Carica l'IPA come artifact
      - name: Upload IPA
        uses: actions/upload-artifact@v3
        with:
          name: myapp-ipa
          path: build/ios/ipa/*.ipa
