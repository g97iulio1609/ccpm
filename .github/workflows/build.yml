# .github/workflows/build-android.yml
name: Build Android AppBundle

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-android:
    runs-on: macos-latest

    steps:
      # 1. Checkout del repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. (Opzionale) Rimuove eventuale riferimento a org.gradle.java.home 
      #    dal file android/gradle.properties se presente
      - name: Remove org.gradle.java.home if present
        run: |
          if [ -f android/gradle.properties ]; then
            sed -i '' '/org.gradle.java.home/d' android/gradle.properties
            echo "Updated android/gradle.properties:"
            cat android/gradle.properties
          fi

      # 3. Configura Java 17 utilizzando l'azione setup-java
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'  # Puoi anche usare 'adoptium' se preferisci
          java-version: '17'
          cache: 'gradle'  # Abilita la cache di Gradle per migliorare le prestazioni

      # 4. Configura Flutter utilizzando l'azione aggiornata
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'

      # 5. Disabilita le analitiche di Flutter (opzionale)
      - name: Disable Flutter Analytics
        run: flutter config --no-analytics

      # 6. Installa le dipendenze di Flutter
      - name: Install dependencies
        run: flutter pub get

      # 7. Costruisci l'Android App Bundle (.aab) in modalità release
      - name: Build Android App Bundle
        run: flutter build appbundle --release

      # 8. Carica l'App Bundle (.aab) come artifact utilizzando la versione v4
      - name: Upload Android AAB
        uses: actions/upload-artifact@v4
        with:
          name: myapp-aab
          path: build/app/outputs/bundle/release/*.aab
